---
title: "Agents"
description: "Prompt files, workspaces, permissions, inheritance, and delegation"
---

Each agent is a named runtime identity from `operator.yaml`. The agent's human-authored prompt lives at `~/.operator/agents/<name>/AGENT.md`.

Use multiple agents when you want different tools, different models, different roles, or a different charter.

## Two Sources Define An Agent

1. `operator.yaml` defines the runnable identity and execution settings.
2. `AGENT.md` defines the human-authored instructions.

Example config:

```yaml
agents:
  operator:
    permissions:
      tools: "*"
      skills: "*"
    transport:
      type: slack
      env:
        bot_token: SLACK_BOT_TOKEN
        app_token: SLACK_APP_TOKEN

  researcher:
    models:
      - "openai/o3"
    thinking: high
    permissions:
      tools: [read_file, write_file, list_files, web_fetch, read_skill, run_skill]
      skills: [docs-auditor]
```

Example `AGENT.md`:

```yaml
---
description: Deep research agent with web access.
---

You are a research specialist. Work methodically, cite sources in summaries,
and write long-form notes into the workspace when the task needs intermediate artifacts.
```

Important distinction:

- the configured key such as `researcher` is the real runtime name
- `AGENT.md` frontmatter contributes human-facing metadata, especially `description`

If `AGENT.md` is missing or malformed, the agent still exists because config is the source of truth.

## What An Agent Gets

Every agent has its own:

- `AGENT.md`
- workspace at `agents/<name>/workspace/`
- memory at `agents/<name>/memory/`
- state at `agents/<name>/state/`
- tool permission filter
- skill permission filter
- model and execution settings

The runtime also creates `workspace/shared` as a symlink to `~/.operator/shared/`, and by convention each agent writes to `shared/<agent-name>/`.

## Workspace Layout

```text
agents/<name>/workspace/
├── inbox/
├── work/
├── artifacts/
├── tmp/
└── shared -> ~/.operator/shared/
```

Purpose of each directory:

- `inbox/`: inbound attachments and imported reference files
- `work/`: active drafts and intermediate outputs
- `artifacts/`: finished deliverables
- `tmp/`: disposable scratch files
- `shared/`: cross-agent file exchange

When Slack attachments arrive, Operator saves them into `inbox/`. Small images are also sent to the model as inline image content.

## Prompt Assembly

On each run, Operator builds the system prompt in this order:

1. `SYSTEM.md`
2. the active agent's `AGENT.md` body
3. allowed skill metadata
4. known peer agents and their descriptions
5. transport or job context
6. active rules from memory

Why this split exists:

- common behavior lives in `SYSTEM.md`
- the agent charter lives in `AGENT.md`
- rules stay editable without rewriting the base prompt
- the stable prefix is cache-friendly while the dynamic suffix changes per run

## Permissions

Agent permissions are an allowlist.

- no `permissions` block means no tools and no skills
- `tools: "*"` means all built-in and runtime-added tools
- `skills: "*"` means all discovered skills
- lists restrict access to named tools or named skills

Example:

```yaml
permissions:
  tools: [read_file, write_file, list_files, web_fetch, spawn_agent]
  skills: [docs-auditor]
```

Keep public-facing agents narrow. In particular, only trusted agents should get tools like `manage_users`, `run_shell`, or unrestricted file writes.

## Delegation With `spawn_agent`

Use `spawn_agent` when one agent should offload a focused sub-task.

Behavior:

- omitting `agent` starts a fresh run of the current agent
- specifying `agent` switches to the target agent's own prompt, memory, workspace, tools, and permissions
- the child keeps the current run mode, so a chat-triggered sub-agent stays in chat mode and a job-triggered sub-agent stays in job mode
- the child starts with fresh conversation state instead of inheriting the whole parent conversation
- max delegation depth is `3`

This is an agent swap, not a conversation fork.

## Headless Agents

An agent does not need a transport block to be useful.

Headless agents work well for:

- scheduled jobs
- `spawn_agent(...)` delegation
- specialist agents that should not be directly reachable by humans

Current runtime detail: the overall process still needs at least one configured transport somewhere in `operator.yaml` or startup exits.

## Inheritance

Per-agent overrides are optional. When omitted, the agent inherits from `defaults`.

| Setting | Resolution |
| --- | --- |
| `models` | agent override or `defaults.models` |
| `thinking` | agent override or `defaults.thinking` |
| `max_iterations` | agent override or `defaults.max_iterations` |
| `context_ratio` | agent override or `defaults.context_ratio` |
| `max_output_tokens` | agent override or `defaults.max_output_tokens` |

## A Good First Split

If you are adding a second agent, keep the split obvious:

- `operator`: broad executor that talks to people
- `researcher`: web and note-heavy specialist
- `reviewer`: file-reading, diff-checking specialist

That keeps prompts shorter, permissions tighter, and delegation easier to reason about.
