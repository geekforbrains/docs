---
title: "Operator"
description: "Local AI agents for Slack, jobs, tools, and team workflows"
---

<img src="/images/operator_banner.png" alt="Operator" />

# Agents have joined the chat.

Operator is a local runtime for autonomous agents that live in Slack, use tools, remember context, and run scheduled jobs. Slack is the only transport today. Agents, jobs, skills, and memory stay as plain files under `~/.operator/`.

## Scan First

- Slack-native today: DMs and `@mentions` in invited channels.
- Local-first: state lives in `~/.operator/`, with SQLite in `~/.operator/db/operator.db`.
- File-driven: agents use `AGENT.md`, jobs use `JOB.md`, skills use `SKILL.md`, and memory uses markdown under `rules/` and `notes/`.
- Team-aware: every inbound transport message is authenticated against users and roles.
- Model-agnostic: model names use LiteLLM format like `anthropic/claude-sonnet-4-6`.
- Tooling is real: shell, files, web, messaging, jobs, skills, memory, state, and sub-agents.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Install Operator, run `operator init`, and send your first Slack message.
  </Card>
  <Card title="Configuration" icon="gear" href="/configuration">
    Runtime, models, Slack transport, permissions, embeddings, and roles.
  </Card>
  <Card title="Agents" icon="robot" href="/concepts/agents">
    Prompt files, workspaces, prompt assembly, permissions, and delegation.
  </Card>
  <Card title="Jobs" icon="clock" href="/concepts/jobs">
    Cron jobs, hooks, UTC scheduling, transport behavior, and counters.
  </Card>
  <Card title="Tools" icon="wrench" href="/concepts/tools">
    Built-in tools, transport tools, and the behavior that matters in practice.
  </Card>
  <Card title="Slack" icon="message" href="/transports/slack">
    Slack app setup, scopes, events, file support, and in-chat commands.
  </Card>
</CardGroup>

## What You Get

- **Agents as teammates.** Each agent has its own prompt, workspace, model settings, permissions, and optional Slack transport.
- **Fresh isolated sub-agent delegation.** Agents can call `spawn_agent(...)` to break work into focused child runs, including cross-agent delegation.
- **Jobs.** Scheduled markdown-defined jobs run every minute against UTC cron schedules.
- **Users and roles.** Unknown users are rejected. Allowed agents are the union of the user's assigned roles. `admin` is built in.
- **File-backed memory.** Rules are always injected, notes are searched on demand, and expired entries move to `trash/`.
- **Per-agent state.** Agents can keep operational state in JSON files under `agents/<name>/state/`.
- **Fallback chains and simple reasoning levels.** Configure `models:` as a failover list and `thinking:` as `off|low|medium|high`.

## File Layout

```text
~/.operator/
├── operator.yaml              # Main config
├── .env                       # API keys and transport secrets
├── SYSTEM.md                  # Global system preamble
├── shared/                    # Shared files for all agents
├── logs/
│   └── operator.log
├── db/
│   ├── operator.db            # SQLite runtime state
│   └── memory_index.db        # FTS5 / optional vector memory index
├── agents/
│   └── <agent>/
│       ├── AGENT.md
│       ├── workspace/
│       │   ├── inbox/
│       │   ├── work/
│       │   ├── artifacts/
│       │   ├── tmp/
│       │   └── shared -> ~/.operator/shared/
│       ├── memory/
│       │   ├── rules/
│       │   ├── notes/
│       │   └── trash/
│       └── state/
├── jobs/
│   └── <job>/
│       ├── JOB.md
│       └── scripts/
├── skills/
│   └── <skill>/
│       ├── SKILL.md
│       ├── scripts/
│       ├── references/
│       └── assets/
└── memory/
    ├── global/
    │   ├── rules/
    │   ├── notes/
    │   └── trash/
    └── users/
        └── <user>/
            ├── rules/
            ├── notes/
            └── trash/
```

## How A Request Flows

1. A Slack DM, Slack `@mention`, or scheduled job starts a run.
2. Operator loads config, resolves the agent, and assembles the system prompt from `SYSTEM.md`, the agent's `AGENT.md` body, discovered skills, configured peer agents, transport context, and rule memory.
3. It renders creation timestamps onto live `user` messages using the authenticated user's timezone when available, otherwise UTC.
4. The agent runs a model/tool loop until it returns a final answer or hits `max_iterations`.
5. Conversations, identities, and job state are stored in SQLite. File-backed memory and agent state remain on disk.

## Recommended Reading Order

1. [Quickstart](/quickstart)
2. [Configuration](/configuration)
3. [Agents](/concepts/agents)
4. [Slack](/transports/slack)
5. [CLI Overview](/cli/overview)
