---
title: "Changelog"
description: "Release history for Operator"
---

## 0.4.0

### Added

- **Users, roles, and auth** — every inbound message is authenticated against a user database with role-based agent access control
- **Agent permissions** — flat allow-lists for [tools and skills](/concepts/agents#permissions) per agent
- **Skill access tools** — [`read_skill` and `run_skill`](/concepts/tools#skill-access) let restricted agents use skills without shell access
- **Agent sandbox** — [`sandbox`](/concepts/agents#sandbox) config flag confines file tools to the workspace (default) or grants full filesystem access
- **File support** — users can upload images, PDFs, and documents in Slack; images are sent to the model as vision input, other files are saved to `workspace/uploads/`
- **`send_file` tool** — agents can upload files back to chat
- **`operator job run`** — run jobs from the CLI with dedicated transport and logging
- **`manage_users` tool** — admin-only runtime user management

### Fixed

- `send_message` and `send_file` default to the current conversation's channel and thread
- Hook scripts respect shebang interpreter
- Skill script paths expanded correctly for `run_skill`
- `manage_skill list` respects agent skill permissions

## 0.3.0

### Added

- **Agent permissions** — flat allow-lists for tools and skills per agent
- **Shared directory** — `~/.operator/shared/` is symlinked into every agent workspace for cross-agent file sharing
- **Bundled skills** — skills shipped with the package, auto-installed on `operator init`
- **`manage_skill` tool** — agents can create, update, and delete skills at runtime
- **`operator skills reset`** — CLI command to restore bundled skills to their original version
- **Transport-scoped read tools** — Slack transport adds `list_channels`, `read_channel`, and `read_thread`
- **Timezone config** — `defaults.timezone` sets the IANA timezone for timestamps and job scheduling (defaults to UTC)

### Fixed

- Default agent renamed from `hermy`/`default` to `operator`

## 0.2.2

### Fixed

- **Prompt caching** — system prompt is split into a stable prefix and dynamic suffix with Anthropic cache breakpoints so the prefix is reused across turns
- Rolling cache breakpoint on conversation history (penultimate user message) for multi-turn token savings
- Read OpenAI `cached_tokens` from `prompt_tokens_details` for unified cache reporting

### Added

- Per-run ID logging via ContextVar for tracing agent runs in logs
- Usage line now shows cache write tokens, prefixed with `Usage:`

## 0.2.1

### Added

- Per-job model override via `model` field in [JOB.md frontmatter](/concepts/jobs#model-override)

## 0.2.0

### Added

- [`operator init`](/cli/overview#setup) command to scaffold `~/.operator/` with starter config and agent

## 0.1.0

### Added

- Initial public beta release
- LiteLLM-backed agent with model fallback chains
- Slack transport with Socket Mode and thread continuity
- SQLite persistence with WAL mode
- Scheduled jobs with cron, prerun gating, and postrun hooks
- Vector memory with harvesting and semantic search
- Key-value store with TTL support
- Built-in tools (shell, file, web, memory, KV, jobs, messaging, sub-agents) plus transport-specific tools
- Skills discovery from markdown files
- Turn-safe context truncation
- CLI for inspection, service management, and job control
