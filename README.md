# Operator Docs

Mintlify docs site for Operator.

## Notes

- Keep these docs aligned with the current `operator` codebase.
- The docs should move from simple setup first to deeper reference later.
- Public release history lives in GitHub. This site intentionally does not publish a changelog page.
- Navigation lives in `docs.json`.
