# Operator Docs

Mintlify docs site for Operator.

## Notes

- Keep these docs aligned with the current `operator` codebase.
- Start with the shortest setup path, then move deeper into customization and reference.
- Public release history lives in GitHub. This site does not publish a changelog page.
- Navigation lives in `docs.json`.
