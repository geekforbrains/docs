---
title: "CLI Overview"
description: "Command map for the operator CLI"
---

The `operator` command is both the long-running runtime entrypoint and the management CLI.

## Scan First

- Running `operator` with no subcommand starts the runtime.
- Most CLI commands work without the background service running.
- Agent-aware commands resolve the agent from `--agent`, then `OPERATOR_AGENT`, then the default agent in config.
- `operator tools` shows built-in tools only. Transport tools are runtime-only.

## Top-Level Commands

| Command | Purpose |
| --- | --- |
| `operator` | start the runtime |
| `operator init` | scaffold `~/.operator/` |
| `operator setup` | guided onboarding |
| `operator config` | print resolved config as JSON |
| `operator agents` | list configured agents |
| `operator tools` | list built-in tools |
| `operator logs` | tail `operator.log` |

## Command Groups

| Group | Purpose |
| --- | --- |
| `operator user ...` | users, identities, and roles |
| `operator service ...` | launchd/systemd service management |
| `operator job ...` | inspect, enable/disable, and manually run jobs |
| `operator skills ...` | list skills and reset bundled skills |
| `operator kv ...` | inspect the per-agent key-value store |
| `operator memories ...` | inspect stored memories |

## Common Flows

### First install

```sh
pip install operator-ai
operator setup --run
```

### Manual scaffold

```sh
operator init
operator user add gavin --role admin slack U123ABC45
operator
```

### Inspect a running setup

```sh
operator config
operator agents
operator tools
operator logs -n 100
```

### Service management

```sh
operator service install
operator service start
operator service status
```

## Agent Resolution

Commands like `operator kv ...` need an agent name. Resolution order is:

1. explicit `--agent`
2. `OPERATOR_AGENT`
3. first configured agent in `operator.yaml`

If config is missing and you did not provide `--agent`, the command exits with an error.

## Reference Pages

<CardGroup cols={2}>
  <Card title="Runtime Commands" icon="terminal" href="/cli/runtime">
    `operator`, `init`, `setup`, `config`, `agents`, `tools`, and `logs`.
  </Card>
  <Card title="Users" icon="users" href="/cli/users">
    Add users, link identities, inspect roles, and remove users.
  </Card>
  <Card title="Service" icon="server" href="/cli/service">
    Manage launchd or systemd service installation and lifecycle.
  </Card>
  <Card title="Jobs" icon="clock" href="/cli/jobs">
    Inspect, enable, disable, and manually run jobs.
  </Card>
  <Card title="Skills" icon="wand-magic-sparkles" href="/cli/skills">
    List skills and reset bundled skills.
  </Card>
  <Card title="KV Store" icon="database" href="/cli/kv">
    Read and write the per-agent key-value store.
  </Card>
  <Card title="Memories" icon="brain" href="/cli/memories">
    Inspect stored memories and memory counts by scope.
  </Card>
</CardGroup>
